@model ApplicationCore.Models.CabModel

@{
    ViewData["Title"] = "AddCab";
}

<h1>Add new Cab</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Add">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CabTypeName" class="control-label"></label>
                (do not add duplicate cab name)
                <input asp-for="CabTypeName" class="form-control" />
                <span asp-validation-for="CabTypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Home" asp-action="Index">Back to Homepage</a>
</div>